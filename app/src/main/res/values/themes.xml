<?xml version="1.0" encoding="utf-8"?>
<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base theme: edge-to-edge handled in code; Compose draws the real UI. -->
    <style name="Theme.Ruta" parent="android:Theme.Material.Light.NoActionBar">
        <item name="android:statusBarColor">@android:color/transparent</item>
        <item name="android:navigationBarColor">@android:color/transparent</item>
        <item name="android:windowBackground">@color/window_background</item>
        <item name="android:windowLayoutInDisplayCutoutMode" tools:targetApi="o_mr1">shortEdges</item>
    </style>

    <!-- Hosting context for WebViews when "force dark websites" is on: a dark, non-light theme
         so the page sees prefers-color-scheme: dark and algorithmic darkening engages. -->
    <style name="Theme.Ruta.WebDark" parent="android:Theme.Material">
        <item name="android:isLightTheme">false</item>
    </style>

    <style name="Theme.Ruta.Splash" parent="Theme.SplashScreen">
        <item name="windowSplashScreenBackground">@color/window_background</item>
        <item name="windowSplashScreenAnimatedIcon">@drawable/ic_launcher_foreground</item>
        <item name="postSplashScreenTheme">@style/Theme.Ruta</item>
    </style>
</resources>
