package io.github.klppl.ruta.ui.home

/**
 * Play flavor: ship no bundled brand logos (to avoid trademark concerns on Google Play).
 * The launcher and dock fall back to each site's own favicon, fetched at runtime.
 */
fun brandIconRes(id: String): Int? = null
