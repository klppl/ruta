{
  "name": "ruta-content-src",
  "version": "1.0.0",
  "private": true,
  "description": "Page-side content script source for the ruta social browser",
  "type": "module",
  "scripts": {
    "build": "node build.mjs"
  },
  "devDependencies": {
    "esbuild": "^0.24.0"
  }
}
